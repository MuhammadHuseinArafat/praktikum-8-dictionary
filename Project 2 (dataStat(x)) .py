def dataStat (x):
    daftarData = []
    for data in x :
        data_a = sum(x)/len(x)
        data_b = max(x)
        data_c = min(x)
    daftarData.append(data_a)
    daftarData.append(data_b)
    daftarData.append(data_c)
    print (daftarData.append(data_a))
    print (daftarData.append(data_b))
    print (daftarData.append(data_c))
